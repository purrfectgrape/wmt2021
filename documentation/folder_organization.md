- `data`
    - `train`
        - raw
        - preprocessed
    - `dev`
    - `test`

- `scripts`

- `models`
    - directory per model

- `translate`
    - directory per model

- `vocab`
    - directory per model

- `sentencepiece`
    - directory per model

- `configs`
    - configs for model
#  Naming Convensions
- use `ja` and `en` file descriptors
- use hyphens
- add tags for preprocess steps
    - filtered
    - tok
    - 
    - ex: `filtered`, `tok`

# Notes
- get rid of unnecessary data
